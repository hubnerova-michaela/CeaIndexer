<Window x:Class="CeaIndexer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:p="clr-namespace:CeaIndexer.Properties"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="{x:Static p:Resources.MainWindow_Title}" Height="650" Width="1100" 
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>

        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WindowCloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BgBorder" Background="{TemplateBinding Background}" CornerRadius="0,12,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=WindowState}" Value="Maximized">
                                <Setter TargetName="BgBorder" Property="CornerRadius" Value="0"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SidebarIconBtn" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border x:Name="MainContainer" Background="{DynamicResource WindowBgBrush}">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Margin" Value="15"/>
                <Setter Property="CornerRadius" Value="12"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=WindowState}" Value="Maximized">
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="CornerRadius" Value="0"/>
                        <Setter Property="Effect" Value="{x:Null}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="65"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource SidebarBgBrush}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="12,0,0,12"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=WindowState}" Value="Maximized">
                                <Setter Property="CornerRadius" Value="0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,40,0,0">
                        <Button Style="{StaticResource SidebarIconBtn}" Click="BtnHome_Click" ToolTip="{x:Static p:Resources.Btn_Home}">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE80F;" FontSize="22" Foreground="{DynamicResource IconBrush}"/>
                        </Button>
                        <Button Style="{StaticResource SidebarIconBtn}" Click="BtnScanFolder_Click" ToolTip="{x:Static p:Resources.Btn_ScanFolder}" Margin="0,5,0,0">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE838;" FontSize="22" Foreground="{DynamicResource IconBrush}"/>
                        </Button>
                        <Button Style="{StaticResource SidebarIconBtn}" Click="BtnSearchEngine_Click" ToolTip="Vyhledávání" Margin="0,5,0,0">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE721;" FontSize="22" Foreground="{DynamicResource IconBrush}"/>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Margin="0,0,0,15">
                        <Button Style="{StaticResource SidebarIconBtn}" Click="BtnSettings_Click" ToolTip="{x:Static p:Resources.Btn_Settings}">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE713;" FontSize="22" Foreground="{DynamicResource IconBrush}"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="0" VerticalAlignment="Top" Height="35" Background="Transparent" MouseLeftButtonDown="TopBar_MouseLeftButtonDown"/>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="35"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TopBar_MouseLeftButtonDown">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                        <Button x:Name="BtnThemeToggle" Width="46" Height="35" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnThemeToggle_Click" ToolTip="Přepnout motiv">
                            <TextBlock x:Name="IconTheme" FontFamily="Segoe MDL2 Assets" Text="&#xE708;" FontSize="14" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Button>

                        <Button Style="{StaticResource WindowControlButton}" Content="&#xE921;" Click="BtnMinimize_Click" ToolTip="Minimalizovat"/>
                        <Button x:Name="BtnMaximizeIcon" Style="{StaticResource WindowControlButton}" Content="&#xE922;" Click="BtnMaximize_Click" ToolTip="Maximalizovat"/>
                        <Button Style="{StaticResource WindowCloseButton}" Content="&#xE8BB;" Click="BtnClose_Click" ToolTip="Zavřít"/>

                    </StackPanel>
                </Border>

                <ContentControl x:Name="MainContentArea" Grid.Row="1" Margin="10"/>
            </Grid>
        </Grid>
    </Border>
</Window>